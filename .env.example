# --- Deployment Configuration ---
# The IP address or hostname of your production server
DEPLOY_HOST=159.198.70.88

# The user to SSH into the server as (defaults to root if unset)
DEPLOY_USER=root

# --- Application Configuration ---
# API Key for Etherscan (Required for backend to fetch external data in Docker)
ETHERSCAN_V2_API_KEY=your_etherscan_api_key_here

# Token Address to track (Defaults to BZR if unset)
# BZR_TOKEN_ADDRESS=0x85Cb098bdcD3Ca929d2cD18Fc7A2669fF0362242

# --- Local Development ---
# If you need to override the backend URL for the frontend
# VITE_API_URL=http://localhost:3001
